@model Model.LongInstructionDTO
@*Johan Sakshaug*@
@{
    ViewBag.Title = Model.title;
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    @Model.title
                </div>
                <div class="card-body">
                    @Model.text
                </div>
            </div>
        </div>
    </div>
</div>